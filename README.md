# random_color_jqplugin

## options 
* color 1 and color2 are two object that the color chosen between them
* bg and color select the backgroun color and color of elements. default is true
* speed => the rate of switch colors
